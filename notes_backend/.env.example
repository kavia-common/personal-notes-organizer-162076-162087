# Server
# Network interface and port used by the Express server
PORT=3000
HOST=0.0.0.0

# Request body size limit for JSON payloads (e.g., 1mb, 2mb)
JSON_BODY_LIMIT=1mb

# CORS
# Comma-separated list of allowed origins (e.g., http://localhost:4200,https://yourapp.com)
# If not set, defaults to allowing http://localhost:4200 and http://localhost:3000
CORS_ORIGIN=http://localhost:4200,http://127.0.0.1:4200

# Database
# You can either set DB_* or rely on MYSQL_* provided by environment
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=notes_app
DB_USER=root
DB_PASSWORD=yourpassword

# Auth
# IMPORTANT: Change this for any non-local environment
JWT_SECRET=please-change-me
# Number of bcrypt salt rounds for password hashing
BCRYPT_SALT_ROUNDS=10
